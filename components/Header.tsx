import { FC } from "react";
import Heading from "./Heading";

const Header: FC = () => {
   return (
      <header>
         <Heading text="Header" />
      </header>
   );
};

export default Header;
