import { FC } from "react";
import Heading from "./Heading";

const Footer: FC = () => {
   return (
      <footer>
         <Heading text="Footer" />
      </footer>
   );
};

export default Footer;
